<?php
    // Modelo: modelBinario.php
    // Este modelo procesará los valores del formulario

    // Captaremos los valores del formulario y los asignamos a varias variables

    $user = $_POST['usuario'];
    $email = $_POST['correo'];
    $passwd = $_POST['contra'];
    $passwdConfirm = $_POST['contraConfirm'];
    $tipoUser = $_POST['perfiles'];

    // Validación (por el momento no se realizará)
    

?>